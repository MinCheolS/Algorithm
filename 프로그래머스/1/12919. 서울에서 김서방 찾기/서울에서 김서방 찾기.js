function solution(seoul) {
    var answer = '';
    let a, b

    
    for(let i = 0; i < seoul.length; i++) {
       for(let j = 0; j < seoul.length; j++) {    
           a = seoul[j]
           }
       }
    b = seoul.indexOf("Kim")
    
    answer = `김서방은 ${b}에 있다`
    
    return answer;
}